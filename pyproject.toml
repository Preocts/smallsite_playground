[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "smallsite"
version = "0.1.0"
description = "Just a small site"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyramid>=2.1",
]

[dependency-groups]
deploy = [
    "gunicorn>=26.0.0",
]
